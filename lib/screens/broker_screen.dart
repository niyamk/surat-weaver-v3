import 'package:flutter/material.dart';

class BrokerScreen extends StatelessWidget {
  const BrokerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Broker Screen"),
      ),
    );
  }
}
